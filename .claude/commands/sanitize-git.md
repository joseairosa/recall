# sanitize-git

Review all commits on the current branch compared to the parent branch and clean up any AI attribution.

## Objective

1. Identify all commits on this branch that are not on the parent branch (default: `main`)
2. Remove AI attribution from commit messages
3. Ensure author/committer is the configured git user

## Forbidden Patterns to Remove

- "Created by Claude"
- "Generated by AI"
- "Co-authored-by: Claude"
- "Co-authored-by:.*[Cc]laude.*"
- "Co-authored-by:.*[Aa]nthrop.*"
- Any AI/assistant attribution in commit messages or trailers

## Execution Steps

### 1. Identify commits to clean

```bash
git log main..HEAD --oneline
```

### 2. Interactive rebase to amend commits

```bash
git rebase -i main
```

Mark commits needing message cleanup as `reword` or `edit`.

### 3. For each commit with AI attribution

```bash
git commit --amend
git commit --amend --reset-author
```

### 4. Force push after cleanup

```bash
git push origin HEAD --force-with-lease
```

## Alternative: Bulk rewrite

If many commits need author reset without message changes:

```bash
git rebase -i main --exec "git commit --amend --reset-author --no-edit"
```

## Verification

After cleanup, verify no AI attribution remains:

```bash
git log main..HEAD --format="%B" | grep -iE "(claude|anthropic|co-authored-by.*ai|created by.*ai)"
```

Should return empty.

---

Parent branch (default main): $ARGUMENTS
